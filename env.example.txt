# ResumeForge environment reference
#
# Put backend variables in backend/.env.
# Put NEXT_PUBLIC_* and BACKEND_HEALTH_URL in frontend/.env.local.

# Backend runtime
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/resumeforge

# Authentication
# Use different random secrets of at least 32 characters in production.
JWT_ACCESS_SECRET=replace_with_a_long_random_secret
JWT_REFRESH_SECRET=replace_with_a_different_long_random_secret
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# Exact frontend origins used for CORS and generated links
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000

# Google OAuth
# Use the same Web client ID for backend and frontend.
GOOGLE_CLIENT_ID=000000000000-example.apps.googleusercontent.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=000000000000-example.apps.googleusercontent.com

# AI provider: GROQ, GEMINI, or GROQ_FALLBACK
AI_PROVIDER=GROQ_FALLBACK

# Groq
GROQ_API_KEY=gsk_replace_me
GROQ_MODEL=openai/gpt-oss-20b
GROQ_QUALITY_MODEL=openai/gpt-oss-120b

# Gemini
GEMINI_API_KEY=replace_me
GEMINI_MODEL=gemini-2.5-flash
GEMINI_QUALITY_MODEL=gemini-2.5-pro

# Password-reset email (required in production)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=smtp-user
EMAIL_PASS=smtp-password
EMAIL_FROM="ResumeForge <noreply@example.com>"

# Optional uploads
CLOUDINARY_CLOUD_NAME=replace_me
CLOUDINARY_API_KEY=replace_me
CLOUDINARY_API_SECRET=replace_me

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_APP_URL=http://localhost:3000
BACKEND_HEALTH_URL=http://localhost:5000/health

# Optional display label
NEXT_PUBLIC_AI_PROVIDER=GROQ_FALLBACK
