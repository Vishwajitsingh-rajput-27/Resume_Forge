'use client';
// TODO: Full InterestsStep implementation mirrors EducationStep pattern
// Stub for initial scaffold — expand with same card+form pattern
export function InterestsStep() {
  return (
    <div className="py-8 text-center text-[var(--text-muted)]">
      <p className="text-sm">InterestsStep — expand this stub with the same accordion card pattern as EducationStep.</p>
    </div>
  );
}
