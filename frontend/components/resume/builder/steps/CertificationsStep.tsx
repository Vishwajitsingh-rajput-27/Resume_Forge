'use client';
// TODO: Full CertificationsStep implementation mirrors EducationStep pattern
// Stub for initial scaffold — expand with same card+form pattern
export function CertificationsStep() {
  return (
    <div className="py-8 text-center text-[var(--text-muted)]">
      <p className="text-sm">CertificationsStep — expand this stub with the same accordion card pattern as EducationStep.</p>
    </div>
  );
}
