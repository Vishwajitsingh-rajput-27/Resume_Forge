'use client';
// TODO: Full LanguagesStep implementation mirrors EducationStep pattern
// Stub for initial scaffold — expand with same card+form pattern
export function LanguagesStep() {
  return (
    <div className="py-8 text-center text-[var(--text-muted)]">
      <p className="text-sm">LanguagesStep — expand this stub with the same accordion card pattern as EducationStep.</p>
    </div>
  );
}
